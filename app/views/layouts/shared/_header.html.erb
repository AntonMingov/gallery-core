<header class="fixed top-0 z-20 w-full">
  <nav class="mx-auto max-w-7xl px-6 py-8 lg:px-10 xl:px-10 2xl:px-0">
    <div class="flex items-center justify-between">
      <%= link_to t('header.title'), root_path, class: 'text-2xl font-light tracking-widest text-white' %>

      <!-- Language switch -->
      <div class="flex items-center text-white text-sm gap-8">
        <% if request.path == root_path %>
          <div>
            <a href="#contact" class="relative scroll-smooth py-1.5 text-white before:absolute before:inset-0 before:origin-bottom before:scale-y-[.03] before:bg-white/60 before:transition before:duration-300 hover:before:scale-y-100 hover:before:scale-x-125 hover:before:bg-white/10">
              <span class="relative"> <%= t('header.contact') %> </span>
            </a>
          </div>
        <% end %>

        <% if I18n.locale == :bg %>
          <%= link_to url_for(locale: :en), class: 'flex items-center hover:underline space-x-1' do %>
            <span>EN</span>
            <span>🇬🇧</span>
          <% end %>
        <% else %>
          <%= link_to url_for(locale: :bg), class: 'flex items-center hover:underline space-x-1' do %>
            <span class="">BG</span>
            <span>🇧🇬</span>
          <% end %>
        <% end %>
      </div>

    </div>
  </nav>
</header>
